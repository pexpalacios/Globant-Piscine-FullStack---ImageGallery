networks:
  app-network:
    driver: bridge

services:
  backend:
    build: ./back
    ports:
      - "3000:3000"
    volumes:
      - ./back:/app
      - /app/node_modules
    networks:
      - app-network
    restart: unless-stopped
  web:
    build: ./front
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    networks:
      - app-network
    depends_on:
      - backend
